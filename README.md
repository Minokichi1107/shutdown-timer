# Shutdown Timer (Batch)

指定した分数後に Windows をシャットダウンするバッチファイルです。

## Usage
- 引数指定：
  shutdown_timer.bat 30  
  → 30分後にシャットダウン

- 入力式：
  shutdown_timer_prompt.bat を実行して分数を入力

## Cancel
シャットダウンを中止する場合：
shutdown /a
